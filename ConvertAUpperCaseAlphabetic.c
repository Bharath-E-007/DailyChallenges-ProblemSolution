boundedArray getColumn(int R, int C, int matrix[] [C], int K)

{

struct BoundedArray result (struct BoundedArray")malloc(sizeof(struct

BoundedArray)); result->arr=(int*)malloc(sizeof(int)"R);

result->SIZE-R;

for (int i=0;i<n;i++){

}

result->arr[i]=matrix[k-1][i];

return result;